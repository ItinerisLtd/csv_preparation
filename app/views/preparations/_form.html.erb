<%= form_with(model: preparation, local: true, method: :post) do |form| %>
  <% if preparation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(preparation.errors.count, "error") %> prohibited this preparation from being saved:</h2>

      <ul>
      <% preparation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <br/>
    <%= form.text_area :content, cols: 130, rows: 40 %>
  </div>

  <br/>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
